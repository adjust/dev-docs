---
export interface Props {
  version: string;
}

const { version } = Astro.props as Props;
---

<sdk-version-block
  class:list={[version, "hidden"]}
  role="SdkVersionSelector"
  data-message={version}
>
  <slot />
</sdk-version-block>
