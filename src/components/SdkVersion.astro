---
interface Props {
  version: string;
}

const { version } = Astro.props;
---

<sdk-version-block
  class:list={[version, "hidden"]}
  role="SdkVersionSelector"
  data-message={version}
>
  <slot />
</sdk-version-block>
