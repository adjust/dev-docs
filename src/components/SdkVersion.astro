---
import VersionComponent from "./Version/SdkVersionComponent";

const { version } = Astro.props;
---

<VersionComponent client:only="react" version={version}>
  <slot slot="content" />
</VersionComponent>
