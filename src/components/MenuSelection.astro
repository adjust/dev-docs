---
let content = "";

if (Astro.slots.has("default")) {
  content = await Astro.slots.render("default");
}

let updatedText = content.replaceAll("—&gt;", "‣")
---

<span class="menuselection" set:html={updatedText} />
