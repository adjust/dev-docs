---
import VersionComponent from "./Version/ApiVersionComponent";

const { version } = Astro.props;
---

<VersionComponent client:only="react" version={version}>
  <slot slot="content" />
</VersionComponent>
