---
interface Props {
  version: string;
}

const { version } = Astro.props;
---

<api-version-block
  class:list={[version, "hidden"]}
  role="ApiVersionSelector"
  data-message={version}
>
  <slot />
</api-version-block>
