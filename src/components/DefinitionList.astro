---
import { extractDefinitionList } from "./utils/extractDefinitionList";

let content = "";

if (Astro.slots.has("default")) {
  content = await Astro.slots.render("default");
}

const definitionList = await extractDefinitionList(content);
---

<Fragment set:html={definitionList} />
