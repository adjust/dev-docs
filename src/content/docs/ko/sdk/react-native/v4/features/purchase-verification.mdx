---
title: 구매 검증
description: App Store 및 Play Store 구매 검증
slug: ko/sdk/react-native/v4/features/purchase-verification
versions:
   - label: v5
     value: v5
     default: true
   - label: v4
     value: v4
redirects:
   v5: /ko/sdk/react-native/features/purchase-verification
---

[구매 검증](https://help.adjust.com/ko/article/purchase-verification)을 활성화한 경우 검증을 위해 구매 이벤트와 함께 추가 정보를 전송해야 합니다. Adjust의 서버가 이벤트 객체에서 이 정보를 수신하면, 포함된 정보가 무엇인지에 따라 이를Apple 또는 Google로 전달합니다.

<Callout type="seealso">

`AdjustEvent` 클래스에 대한 자세한 내용은 [이벤트 정보 전송](/ko/sdk/react-native/features/events)을 참조하시기 바랍니다.

</Callout>
<Tabs> <Tab title="App Store" icon="StoreAppStore" sync="appstore">

앱 스토어 구매를 검증하려면 `AdjustEvent` 객체를 이벤트 토큰으로 인스턴스화하고 다음의 속성을 설정합니다.

- `productId` \(`string`\): 성공적으로 구매된 아이템의 제품 ID.
- `transactionId` \(`string`\): 인증할 거래의 ID.
- `receipt` \(`string`\): 인앱 구매 영수증.

```js
var adjustEvent = new AdjustEvent("abc123");
adjustEvent.setRevenue(6.0, "EUR");
adjustEvent.setProductId("product-id");
adjustEvent.setTransactionId("transaction-id");
adjustEvent.setReceipt("receipt");
Adjust.trackEvent(adjustEvent);
```

</Tab>
<Tab title="Play Store" icon="PlatformGooglePlay" sync="playstore">

Play Store 구매를 검증하려면 `AdjustEvent` 객체를 이벤트 토큰으로 인스턴스화하고 다음의 속성을 설정합니다.

- `productId` \(`string`\): 성공적으로 구매된 아이템의 제품 ID.
- `purchaseToken` \(`string`\): 성공적으로 완료된 인앱 구매에 대해 생성된 구매 토큰.

```js
var adjustEvent = new AdjustEvent("abc123");
adjustEvent.setRevenue(6.0, "EUR");
adjustEvent.setProductId("product-id");
adjustEvent.setPurchaseToken("purchase-token");
Adjust.trackEvent(adjustEvent);
```

</Tab>
</Tabs>
