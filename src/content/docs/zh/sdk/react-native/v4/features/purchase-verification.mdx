---
title: "收入验证"
description: "验证App Store 和 Play 商店中的购买情况"
slug: "en/sdk/react-native/v4/features/purchase-verification"
versions:
   - label: "v5"
     value: "v5"
     default: true
   - label: "v4"
     value: "v4"
redirects:
   v5: "/en/sdk/react-native/features/purchase-verification"
---

如果您已启用[购买验证](https://help.adjust.com/en/article/purchase-verification)，则必须随购买事件一同发送额外信息用于验证。Adjust 服务器在事件对象中接收到此信息后，会将其转发给 Apple 或 Google 以根据所包含的信息来验证购买。

<callout type="seealso">

请参阅[发送事件信息](/en/sdk/react-native/features/events)了解有关`AdjustEvent`类的更多信息。

</Callout> <tabs> <tab title="App Store" icon="StoreAppStore" sync="appstore">

要验证 App Store 购买，请使用您的事件识别码实例化`AdjustEvent`对象并设置以下属性：

-  `productId` \(`string`\)：已被成功售出货品的产品识别码。
-  `transactionId` \( `string` \)：要验证的交易 ID。
-  `receipt` \( `string` \)：应用内购买收据。

```js
var adjustEvent = new AdjustEvent("abc123");
adjustEvent.setRevenue(6.0, "EUR");
adjustEvent.setProductId("product-id");
adjustEvent.setTransactionId("transaction-id");
adjustEvent.setReceipt("receipt");
Adjust.trackEvent(adjustEvent);
```

</Tab> <tab title="Play 商店" icon="PlatformGooglePlay" sync="playstore">

要验证 Play 商店购买，请使用您的事件识别码实例化一个`AdjustEvent`对象并设置以下属性：

-  `productId` \(`string`\)：已被成功售出货品的产品识别码。
-  `purchaseToken` \(`string`\)：已成功完成的应用内购买的购买识别码。

```js
var adjustEvent = new AdjustEvent("abc123");
adjustEvent.setRevenue(6.0, "EUR");
adjustEvent.setProductId("product-id");
adjustEvent.setPurchaseToken("purchase-token");
Adjust.trackEvent(adjustEvent);
```

</Tab> </Tabs>
