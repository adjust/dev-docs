---
title: "アプリエンドポイント"
description: "アプリエンドポイントを使用して、新規アプリの作成と更新を自動化します。"
slug: "ja/api/app-automation-api/app-endpoint"
sidebar-position: 2
---

`/app`エンドポイントを使用して、新規アプリの作成と更新を自動化します。

## 新規アプリを作成する

テンプレートアプリの設定を使用して新規アプリを作成します。アプリがすでにある場合は、`force_update`パラメーターを使用して新しい設定に更新できます。

```http
POST https://settings.adjust.com/api/app
```

### パラメータ

<Table search>

| パラメーター             | データタイプ | 入力する値 | 説明                                                                                                                                   |
| ------------------------ | ------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `store_id`\*             | 文字列       | ボディ     | バンドルID（Android） \| ストアID（Apple）                                                                                             |
| `platform`\*             | 文字列       | ボディ     | `android` \| `ios`                                                                                                                     |
| `name`\*                 | 文字列       | ボディ     | アプリの名前。                                                                                                                         |
| `bundle_id`\*            | 文字列       | ボディ     | アプリのバンドルID。                                                                                                                   |
| `android_signature_hash` | 文字列       | ボディ     | SHA1 ハッシュ値。SDKシグネチャーをビルドするため生成されるキーストアファイル。                                                         |
| `channel_setup`          | オブジェクト | ボディ     | アプリのパートナーモジュール設定に必要です。詳細は[チャネル設定に関する記事](/ja/api/app-automation-api/channel-setup)をご覧ください。 |
| `editors`                | 文字列       | ボディ     | アプリにアクセスできるユーザーのメールアドレスのリスト。                                                                               |
| `template_app_token`     | 文字列       | ボディ     | テンプレートとして使用するAdjustのアプリトークン。テンプレートアプリを使用していない場合に必要です。                                   |
| `force_update`           | Boolean      | ボディ     | この値をtrueとしてパスし、既存のアプリの設定を更新します。                                                                             |

</Table>

### レスポンスパラメーター

<Table>

| パラメーター       | データタイプ | 説明                                                                                                                                                             |
| ------------------ | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `adjust_app_token` | 文字列       | SDKの実装と他のAPIリクエストに使用するAdjustアプリトークン。                                                                                                     |
| `name`             | 文字列       | アプリの名前。                                                                                                                                                   |
| `sk_app_id`        | 整数         | iOSアプリのStoreKitアプリID。                                                                                                                                    |
| `store_id`         | 文字列       | AndroidのバンドルID \| AppleのストアID                                                                                                                           |
| `bundle_id`        | 文字列       | AndroidまたはiOSアプリのバンドルID。                                                                                                                             |
| `ticket_token`     | UUID         | 現在のジョブステータスを確認するためのチケットID。詳細は[ステータスエンドポイントのドキュメント](/ja/api/app-automation-api/status-endpoint)を参照してください。 |

</Table>

### 例

```console
$ curl \
--header "AdjustAuthorization: Token <adjust api token id="sl-md0000000">" \
--header "Content-Type: application/json" \
--data '{ "name":"Test App Name", "bundle_id":"test.bundle.app", "store_id":"test.bundle.app", "platform":"android", "channel_setup": { "facebook": {"app_id": "123456789"} } }' \
-X POST https://settings.adjust.com/api/app
```

```json
{
   "adjust_app_token": "p77yk727r18g",
   "name": "Test App Name",
   "sk_app_id": null,
   "store_id": "test.bundle.app",
   "bundle_id": "test.bundle.app",
   "ticket_token": "af7702a9-ba95-6ba2-bdac-3f83e55ed8d3"
}
```

## アプリの詳細を取得する

アプリストアIDとプラットフォーム名を使用して、アプリに関する詳細情報を取得します。

```http
GET https://settings.adjust.com/api/app?store_id={}&platform={}
```

### パラメータ

<Table>

| 名前         | データタイプ | 入力する値 | 説明                                   |
| ------------ | ------------ | ---------- | -------------------------------------- |
| `store_id`\* | 文字列       | クエリ     | AndroidのバンドルID \| AppleのストアID |
| `platform`\* | 文字列       | クエリ     | `android` \| `ios`                     |

</Table>

### レスポンスパラメーター

<Table>

| 名前                     | 種類         | 説明                                                                           |
| ------------------------ | ------------ | ------------------------------------------------------------------------------ |
| `name`                   | 文字列       | アプリの名前                                                                   |
| `platform`               | 文字列       | `android` \| `ios`                                                             |
| `store_id`               | 文字列       | AndroidのバンドルID \| iOSのストアID                                           |
| `adjust_app_token`       | 文字列       | リクエストされたアプリのトークン。                                             |
| `additional_info`        | オブジェクト | 以前エンドポイントで受け取ったアプリ情報。                                     |
| `channel_setup`          | オブジェクト | パートナー固有のパラメーター。チャネル設定をご覧ください。                     |
| `cross_promo_token`      | 文字列       | クロスプロモーションに使用されるAdjustトラッカートークン（利用可能になり次第） |
| `android_signature_hash` | 文字列       | SHA1 ハッシュ値。SDKシグネチャーをビルドするため生成されるキーストアファイル   |

</Table>

### 例

```console
$ curl \
--header "AdjustAuthorization: Token <adjust api token id="sl-md0000000">" \
-L -X GET 'https://settings.adjust.com/api/app?store_id=test.bundle.app&platform=android'
```

```json
{
   "name": "Test App Name",
   "platform": "android",
   "store_id": "test.bundle.app",
   "adjust_app_token": "p77yk727r18g",
   "additional_info": { "overrides": {} },
   "channel_setup": {
      "facebook": { "app_id": "123456789" },
      "snapchat": { "app_id": "test.bundle.app" }
   },
   "cross_promo_token": "hgj3li1",
   "android_signature_hash": "FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF"
}
```

## Adjustトークンでアプリの詳細を取得する

Adjustアプリトークンを使用して、アプリに関する詳細情報を取得します。

```http
GET https://settings.adjust.com/api/app/{adjust_app_token}
```

### パラメータ

<Table>

| 名前                 | データタイプ | 入力する値 | 説明                                                                                                |
| -------------------- | ------------ | ---------- | --------------------------------------------------------------------------------------------------- |
| `adjust_app_token`\* | 文字列       | パス       | Adjustアプリトークン。このトークンは、Adjustダッシュボードまたは/app POSTレスポンスで確認できます。 |

</Table>

### レスポンスパラメーター

<Table>

| 名前                     | 種類         | 説明                                                                           |
| ------------------------ | ------------ | ------------------------------------------------------------------------------ |
| `name`                   | 文字列       | アプリの名前                                                                   |
| `platform`               | 文字列       | `android` または `ios`                                                         |
| `store_id`               | 文字列       | AndroidのバンドルID \| iOSのストアID                                           |
| `adjust_app_token`       | 文字列       | リクエストされたアプリのトークン。                                             |
| `additional_info`        | オブジェクト | 以前エンドポイントで受け取ったアプリ情報。                                     |
| `channel_setup`          | オブジェクト | パートナー固有のパラメーター。チャネル設定をご覧ください。                     |
| `cross_promo_token`      | 文字列       | クロスプロモーションに使用されるAdjustトラッカートークン（利用可能になり次第） |
| `android_signature_hash` | 文字列       | SHA1 ハッシュ値。SDKシグネチャーをビルドするため生成されるキーストアファイル   |

</Table>

### 例

```console
$ curl \
--header "AdjustAuthorization: Token <adjust api token id="sl-md0000000">" \
-X GET https://settings.adjust.com/api/app/p77yk727r18g/
```

```json
{
   "name": "Test App Name",
   "platform": "android",
   "store_id": "test.bundle.app",
   "adjust_app_token": "p77yk727r18g",
   "additional_info": { "overrides": {} },
   "channel_setup": {
      "facebook": { "app_id": "123456789" },
      "snapchat": { "app_id": "test.bundle.app" }
   },
   "cross_promo_token": "hgj3li1",
   "android_signature_hash": "FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF:FF"
}
```

## アプリのチャネルを取得する

アプリのアクティブトラッカーを持つチャネルのリストを取得します。

```http
GET https://settings.adjust.com/api/app/{adjust_app_token}/channels
```

### パラメータ

<Table>

| 名前                 | データタイプ | 入力する値 | 説明                                                                                                |
| -------------------- | ------------ | ---------- | --------------------------------------------------------------------------------------------------- |
| `adjust_app_token`\* | 文字列       | パス       | Adjustアプリトークン。このトークンは、Adjustダッシュボードまたは/app POSTレスポンスで確認できます。 |

</Table>

### レスポンスパラメーター

<Table>

| パラメーター | データタイプ | 説明                                                       |
| ------------ | ------------ | ---------------------------------------------------------- |
| `channels`   | 配列<文字列> | このアプリのアクティブチャネルのネットワークslugのリスト。 |

</Table>

<Accordion title="ネットワークスラグ">

-  `apple_search_ads`
-  `applovin`
-  `chartboost`
-  `crossinstall`
-  `crosspromotion`
-  `digitalturbine`
-  `facebook`
-  `gameanalytics`
-  `google`
-  `ironsource`
-  `snapchat`
-  `tapjoy`
-  `unityads`
-  `vungle`
-  `inmobi`

</Accordion>

### 例

```console
$ curl \
--header 'AdjustAuthorization: Token <adjust api token id="sl-md0000000">' \
--header "SignatureAuthorization: Token <adjust signature token id="sl-md0000000">" \
--X GET 'https://settings.adjust.com/api/app/927t3s86rzsw/channels'
```

```json
[
   "unityads",
   "digitalturbine",
   "jetfuel_api",
   "jetfuel",
   "criteo",
   "tapjoy",
   "bytedance",
   "moloco",
   "adikteev",
   "pinsightmedia",
   "applovin",
   "aura_ironsource",
   "freakout",
   "tradedoubler"
]
```
