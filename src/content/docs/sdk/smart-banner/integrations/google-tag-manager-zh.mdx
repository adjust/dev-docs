---
title: "智能横幅 SDK Goolge 跟踪代码管理器集成"
description: "将 Adjust 智能横幅 SDK 集成至 Google 跟踪代码管理器来通过 GTM 容器发送智能横幅。"
slug: "en/sdk/smart-banner/integrations/google-tag-manager"
sidebar-label: "Goolge 跟踪代码管理器"
sidebar-position: 3
---
您可以将 Adjust 智能横幅 SDK 集成至 Google 跟踪代码管理器，通过 GTM 容器发布智能横幅。请按照本指南中的步骤进行设置。

[在 Google 跟踪代码管理器中集成 SDK](integrate-the-sdk-in-google-tag-manager)
------------------------------------------------------------------

1. 找到 Google 跟踪代码管理器。

2. 选择 **Tags → New** 。

3. 选择 **Tag Configuration \(跟踪代码配置\)** ，然后选择 **Custom HTML \(自定义 HTML\)** 。

4. 在[HTML](/en/sdk/smart-banner#using-a-cdn) 字段中添加 **SDK 加载片段，确保** **Firing Triggers \(触发型触发器\)** 字段设置为 **Initialization \- All Pages \(初始化 \- 所有页面\)** 。

   ![A screenshot of the GTM tag configuration screen](@images/smart-banner-sdk/gtm/1-gtm-configuration.png)
5. 添加另一个自定义 HTML 标签并在`<script id="sl-md0000000"></script>`标签之间输入 SDK 初始化调用。该调用必须包含`appToken` 。请查看[SDK 初始化选项](/en/sdk/smart-banner/init-options)来使用`init`方法配置 SDK。

   ```js
   window.AdjustSmartBanner.init({ appToken: "TOKEN" });
   ```

   ![A screenshot of the custom HTML editor with init options](@images/smart-banner-sdk/gtm/2-init-options.png)
6. 选择屏幕右上角的加号图标 \( `+` \) 即可创建新的触发器。

7. 找到 **Trigger Configuration \(触发器配置\)** ，然后选择 **Page View \(页面视图\)** 来在每次页面查看时加载脚本。您可以设置是在每个页面上触发初始化还是输入特定的主机名。

   ![A screenshot of the firing trigger configuration](@images/smart-banner-sdk/gtm/3-trigger-config.png)
8. 保存触发器配置，然后找到 **Tag Configuration \(跟踪代码配置\)** 。

9. 请确保所有配置正确，然后发布您的 GTM 容器。

