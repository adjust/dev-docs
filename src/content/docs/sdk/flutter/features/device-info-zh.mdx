---
title: "获取设备信息"
description: "用这些方法来向回传添加细节信息，优化报告。"
slug: "en/sdk/flutter/features/device-info"
---
Adjust SDK 包含能够返回设备信息的辅助方法。用这些方法来向回传添加细节信息，优化报告。

[Adjust 设备标识符](adjust-device-identifier)
----------------------------------------

<codeblock title="方法签名"> 

```dart
static Future<String?> getAdid() async
```

</CodeBlock> 

Adjust 会针对每台设备生成一个独特的 Adjust 设备 ID \(ADID\)。调用 `getAdid` 方法来通过 `String` 的形式返回此 ID。

```dart
Adjust.getAdid().then((adid) {
  // Use adid string value.
});
```

[广告主 ID](id-for-advertisers)
----------------------------

<codeblock title="方法签名"> 

```dart
static Future<String?> getIdfa() async
```

</CodeBlock> 

广告主 ID \(IDFA\) 是 Apple 设备的独特标识符。调用 `getIdfa` 方法来通过 `String` 的形式返回此 ID。

```dart
Adjust.getIdfa().then((idfa) {
  // Use idfa string value.
});
```

[供应商 ID](id-for-vendor)
-----------------------

<codeblock title="方法签名"> 

```dart
static Future<String?> getIdfv() async
```

</CodeBlock> 

供应商 ID \(IDFV\) 是 Apple 设备的独特标识符。您可以使用回传函数调用`Adjust.getIdfv`方法来返回设备的 IDFV。SDK 会异步获取信息并将其传递给您的回传函数。

```dart
Adjust.getIdfv().then((idfv) {
  // Use idfv string value.
});
```

[Google Play 服务广告 ID](google-play-services-advertising-id)
----------------------------------------------------------

<codeblock title="方法签名"> 

```dart
static Future<String?> getGoogleAdId() async
```

</CodeBlock> 

Google Play 服务广告 ID \(GPS ADID\) 是安卓设备的独特标识符。

用户可以通过启用设备的 "选择停用广告个性化功能" 设置来停止分享自己的 GPS ADID。当用户启用该设置后，Adjust SDK 试图读取 GPS ADID 时会返回一串零。

您可以通过在后台线程中调用 `getGoogleAdId` 方法来获取该值。指定一个委托 \(delgate\) 函数，以访问 GPS ADID 值。

```dart
Adjust.getGoogleAdId().then((googleAdId) {
  // Use googleAdId string value.
});
```

[Amazon 广告主 ID](amazon-advertiser-id)
-------------------------------------

<codeblock title="方法签名"> 

```dart
static Future<String?> getAmazonAdId() async
```

</CodeBlock> 

Amazon 广告 ID \(Amazon Ad ID\) 是安卓设备的独特标识符。调用 `getAmazonAdId` 方法来通过 `String` 的形式返回此 ID。

```dart
Adjust.getAmazonAdId().then((amazonAdId) {
  // Use amazonAdId string value.
});
```

