---
title: 디바이스 정보 확인
description: 해당 메서드를 사용하여 콜백에 세부 정보를 추가하고 리포트를 보완하시기 바랍니다.
slug: ko/sdk/flutter/features/device-info
versions:
   - label: v5
     value: v5
     default: true
   - label: v4
     value: v4
redirects:
   v4: /ko/sdk/flutter/v4/features/device-info
---

Adjust SDK는 디바이스 정보를 반환하는 헬프 메서드가 포함되어 있습니다. 해당 메서드를 사용하여 콜백에 세부 정보를 추가하고 리포트를 보완하시기 바랍니다.

## Adjust 디바이스 ID {#adjust-device-identifier}

<CodeBlock title="메서드 서명">

```dart
static Future<String?> getAdid() async
```

</CodeBlock>

Adjust는 각 기기에 대해 고유한 ADID\(Adjust 기기 ID\)를 생성합니다. `getAdid` 메서드를 호출하면 이 ID가 `String`로 반환됩니다.

```dart
Adjust.getAdid().then((adid) {
  // Use adid string value.
});
```

## 광고주용 ID {#id-for-advertisers}

<CodeBlock title="메서드 서명">

```dart
static Future<String?> getIdfa() async
```

</CodeBlock>

IDFA\(광고주용 ID\)는 Apple 기기에 대한 기기별 고유 ID입니다. `getIdfa` 메서드를 호출하면 이 ID가 `String`로 반환됩니다.

```dart
Adjust.getIdfa().then((idfa) {
  // Use idfa string value.
});
```

## 벤더용 ID {#id-for-vendor}

<CodeBlock title="메서드 서명">

```dart
static Future<String?> getIdfv() async
```

</CodeBlock>

IDFA\(벤더용 ID\)는 Apple 기기에 대한 기기별 고유 ID입니다. 콜백 함수와 함께 `Adjust.getIdfv` 메서드를 호출하여 기기의 IDFV를 반환할 수 있습니다. SDK는 비동기적으로 정보를 가져와 이 정보를 콜백 함수에 전달합니다.

```dart
Adjust.getIdfv().then((idfv) {
  // Use idfv string value.
});
```

## Google Play Services Advertising ID {#google-play-services-advertising-id}

<CodeBlock title="메서드 서명">

```dart
static Future<String?> getGoogleAdId() async
```

</CodeBlock>

GPS ADID\(Google 플레이 서비스 광고 ID\)는 Android 기기에 대한 기기별 고유 ID입니다.

사용자는 기기에서 "광고 개인 최적화 사용 안 함\(Opt out of Ads Personalization\)"을 설정하고 GPS ADID 공유를 옵트아웃할 수 있습니다. 사용자가 이 설정을 활성화하면, 앱에서 GPS ADID 읽기 시도 시 Adjust SDK가 0으로 구성된 문자열을 반환합니다.

이 값에 액세스하려면 background thread에서 `getGoogleAdId` 메서드를 호출해야 합니다. GPS ADID 값에 액세스하기 위한 델리게이트 함수를 할당합니다.

```dart
Adjust.getGoogleAdId().then((googleAdId) {
  // Use googleAdId string value.
});
```

## Amazon Advertiser ID {#amazon-advertiser-id}

<CodeBlock title="메서드 서명">

```dart
static Future<String?> getAmazonAdId() async
```

</CodeBlock>

Amazon Ad ID\(Amazon 광고 ID\)는 Android 기기에 대한 기기별 고유 ID입니다. `getAmazonAdId` 메서드를 호출하면 이 ID가 `String`로 반환됩니다.

```dart
Adjust.getAmazonAdId().then((amazonAdId) {
  // Use amazonAdId string value.
});
```
