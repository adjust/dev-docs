---
title: "Meta Install Referrer"
description: "Use the Meta Install Referrer plugin to send Meta Install Referrer information to Adjust."
slug: "en/sdk/android/plugins/meta-referrer-plugin"
multiVersion: true
---
<sdkversion version="v5"> <callout type="note"> 

The Meta Install Referrer plugin bundles the `<queries id="sl-md0000000">` elements [required by Meta](https://developers.facebook.com/docs/app-ads/meta-install-referrer#step-1--add-the-meta-apps). You don't need to add these to your `AndroidManifest.xml`.
</Callout> 

The Meta Install Referrer plugin enables the Adjust SDK to read Meta Install Referrer values on a device. The SDK then sends this information to Adjust's servers as part of the install payload.

To add the plugin to your app:

1. `build.gradle`ファイルに以下のdependencyを追加します：

   <codeblock title="build.gradle"> 

   ```groovy
   dependencies {
      implementation 'com.adjust.sdk:adjust-android-meta-referrer:$ANDROID_V5_VERSION'
   }
   ```

   </CodeBlock> 
2. [App Dashboard](https://developers.facebook.com/apps)でMetaアプリIDを検索します。詳しくはMetaの[App Dashboardのドキュメント](https://developers.facebook.com/docs/development/create-an-app/app-dashboard)をご覧ください。

3. `AdjustConfig.setFbAppId`メソッドに`String`引数としてアプリIDをパスします。

<tabs> <tab title="Kotlin"> <codeblock highlight="{range: 2}"> 

```kotlin
val config = AdjustConfig(this, appToken, environment)
config.setFbAppId(<fb_app_id_string id="sl-md0000000">)
Adjust.initSdk(config)
```

</CodeBlock> </Tab> <tab title="Java"> <codeblock highlight="{range: 2}"> 

```java
AdjustConfig config = new AdjustConfig(this, appToken, environment);
config.setFbAppId(<fb_app_id_string id="sl-md0000000">);
Adjust.initSdk(config);
```

</CodeBlock> </Tab> </Tabs> </SdkVersion> <sdkversion version="v4"> 

The Meta Install Referrer plugin is available only for SDK v5\. Check out the [migration guide](/en/sdk/android/migration/v4-to-v5) for more information
</SdkVersion>

