---
title: OAIDプラグイン
description: OAIDプラグインを使用してデバイスのOAIDを読み取ります。
slug: ja/sdk/react-native/plugins/oaid
versions:
   - label: v5
     value: v5
     default: true
   - label: v4
     value: v4
redirects:
   v4: /ja/sdk/react-native/v4/plugins/oaid
---

Open Anonymous Device Identifier（OAID）は、Androidデバイスの一意の識別子です。Mobile Security Alliance（MSA）は、中国で製造される全デバイスがOAIDを提供するよう推奨しています。OAIDを使用することで、Google Play Servicesが利用できないデバイスをアトリビュートおよび計測できます。

OAIDプラグインは、デバイスのOAIDを提供します。SDKは、OAIDだけでなく他の識別子も引き続き使用できます。

## プラグインをアプリに追加する {#add-the-plugin-to-your-app}

<Callout type="note">

Adjustプレハブの **前に** Adjust OAIDプレハブを読み込む必要があります。これにより、SDKが初期化される前にプラグインが命令を読み取ることが保証されます。

</Callout>

React Native向けのAdjust OAIDプラグインはnpmから入手できます。

<CodeBlock title="NPM">

```console
$ npm install react-native-adjust-oaid --save
```

</CodeBlock>

## プラグインを使用する {#use-the-plugin}

プラグインを設定したら、デバイスのIMEIとMEIDを収集できます。これを行うには、`AdjustOaid.ReadOaid`メソッドをAdjust SDKの開始前に呼び出します。

```jsx
import { AdjustOaid } from "react-native-adjust-oaid";
AdjustOaid.readOaid();
// ...
Adjust.create(adjustConfig);
```

SDKがOAID値を読み取らないようにするには、`AdjustOaid.doNotReadOaid()`メソッドを呼び出します。

```jsx
import { AdjustOaid } from "react-native-adjust-oaid";
AdjustOaid.doNotReadOaid();
// ...
Adjust.create(adjustConfig);
```
