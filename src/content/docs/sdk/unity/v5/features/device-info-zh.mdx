---
title: 获取设备信息
description: 用这些方法来向回传添加细节信息，优化报告。
slug: zh/sdk/unity/features/device-info
versions:
   - label: v5
     value: v5
     default: true
   - label: v4
     value: v4
redirects:
   v4: /zh/sdk/unity/v4/features/device-info
---

Adjust SDK 包含能够返回设备信息的辅助方法。用这些方法来向回传添加细节信息，优化报告。

## Adjust 设备标识符 {#adjust-device-identifier}

```cs
public static void GetAdid(Action<string> callback)
```

Adjust 会针对每台设备生成一个独特的 Adjust 设备 ID \(ADID\)。您可以使用回传函数调用`Adjust.GetAdid`方法来返回设备的 ADID。SDK 会异步获取信息并将其传递给您的回传函数。

```cs
Adjust.GetAdid(adid =>
{
    // Your callback function
});
```

## 广告主 ID {#id-for-advertisers}

<CodeBlock title="方法签名">

```cs
public static void GetIdfa(Action<string> callback)
```

</CodeBlock>

广告主 ID \(IDFA\) 是 Apple 设备的独特标识符。您可以使用回传函数调用`Adjust.GetIdfa`方法来返回设备的 IDFA。SDK 会异步获取信息并将其传递给您的回传函数。

```cs
Adjust.GetIdfa(idfa =>
{
   // Your callback function
});
```

## 供应商 ID {#id-for-vendor}

<CodeBlock title="方法签名">

```cs
public static void GetIdfv(Action<string> callback)
```

</CodeBlock>

供应商 ID \(IDFV\) 是 Apple 设备的独特标识符。您可以使用回传函数调用`Adjust.GetIdfv`方法来返回设备的 IDFV。SDK 会异步获取信息并将其传递给您的回传函数。

```cs
Adjust.GetIdfv(idfv =>
{
    // Your callback function
});
```

## Google Play 服务广告 ID {#google-play-services-advertising-id}

<CodeBlock title="方法签名">

```cs
public static void GetGoogleAdId(Action<string> OnDeviceIdsRead)
```

</CodeBlock>

Google Play 服务广告 ID \(GPS ADID\) 是安卓设备的独特标识符。

用户可以通过启用设备的 "选择停用广告个性化功能" 设置来停止分享自己的 GPS ADID。当用户启用该设置后，Adjust SDK 试图读取 GPS ADID 时会返回一串零。

您可以通过在后台线程中调用 `GetGoogleAdId` 方法来获取该值。指定一个委托 \(delgate\) 函数，以访问 GPS ADID 值。

```cs
Adjust.GetGoogleAdId(googleAdId => {
   // Your callback function
});
```

## Amazon 广告主 ID {#amazon-advertiser-id}

<CodeBlock title="方法签名">

```cs
public static void GetAmazonAdId(Action<string> onAmazonAdIdRead)
```

</CodeBlock>

Amazon 广告 ID \(Amazon Ad ID\) 是安卓设备的独特标识符。使用回传函数调用`GetAmazonAdId`方法。SDK 会异步获取信息并将其传递给您的回传函数。

```cs
Adjust.GetAmazonAdId(amazonAdId => {
   // Your callback function
});
```
