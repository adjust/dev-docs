---
title: AdMob SDK 연동
description: Admob 연동을 사용하여 Admob 정보를 Adjust 서버로 전달합니다.
slug: ko/sdk/unity/v4/integrations/admob
versions:
   - label: v5
     value: v5
     default: true
   - label: v4
     value: v4
redirects:
   v5: /ko/sdk/unity/integrations/admob
---

<MinorVersion added="v4.29.0">

Admob SDK의 광고 매출을 측정하고 싶다면, SDK간 연동을 사용하여 Adjust 서버로 광고 매출 정보를 전달할 수 있습니다.

</MinorVersion>
<Callout type="note">

Admob의 광고 매출 측정에 관한 문의는 담당 계정 매니저나 [support@adjust.com](mailto:support@adjust.com)으로 연락주시기 바랍니다.

</Callout>

## 시작에 앞서 {#before-you-begin}

1. [Adjust SDK를 구성하고 앱에 연동합니다](/ko/sdk/unity).
2. AdMob에서 노출 수준 광고\(ILAR\)를 구성합니다. [자세한 내용은 Google 문서를 참조하시기 바랍니다](https://support.google.com/admob/answer/11322405).

## 예시 {#example}

```cs
this.rewardedAd.OnPaidEvent += this.HandleAdPaidEvent;
public void HandleAdPaidEvent(object sender, AdValueEventArgs args)
{
    // for more information, please check AdMob official docs at:
    // https://developers.google.com/admob/unity/impression-level-ad-revenue
    AdValue adValue = args.AdValue;
    AdapterResponseInfo loadedAdapterResponseInfo = rewardedAd.GetResponseInfo().GetLoadedAdapterResponseInfo();

    // send ad revenue info to Adjust
    AdjustAdRevenue adRevenue = new AdjustAdRevenue(AdjustConfig.AdjustAdRevenueSourceAdMob);
    adRevenue.setRevenue(adValue.Value / 1000000f, adValue.CurrencyCode);
    adRevenue.setAdRevenueNetwork(loadedAdapterResponseInfo.AdSourceName)
    Adjust.trackAdRevenue(adRevenue);
}
```
