---
title: Meta インストールリファラー
description: Metaインストールリファラープラグインを使用して、 Metaインストールリファラー情報をAdjustに送信します。
slug: ja/sdk/android/plugins/meta-referrer-plugin
versions:
   - label: v5
     value: v5
     default: true
   - label: v4
     value: v4
redirects:
   v4: /ja/sdk/android/v4/plugins/meta-referrer-plugin
---

<Callout type="note">

Metaインストールリファラープラグインは、 [Metaが必要とする](https://developers.facebook.com/docs/app-ads/meta-install-referrer#step-1--add-the-meta-apps)`<queries>`要素をバンドルしています。これらを`AndroidManifest.xml`に追加する必要はありません。

</Callout>

Metaインストールリファラープラグインは、Adjust SDKがデバイス上のMetaインストールリファラー値を読み取ることを可能にします。その後、SDKはこの情報をインストールペイロードの一部として、Adjustサーバーに送信します。

プラグインをアプリに追加する方法：

1. `build.gradle`ファイルに以下のdependencyを追加します：

   <CodeBlock title="build.gradle">

   ```groovy
   dependencies {
      implementation 'com.adjust.sdk:adjust-android-meta-referrer:5.0.0'
   }
   ```

   </CodeBlock>

2. [App Dashboard](https://developers.facebook.com/apps)でMetaアプリIDを検索します。詳しくはMetaの[App Dashboardのドキュメント](https://developers.facebook.com/docs/development/create-an-app/app-dashboard)をご覧ください。

3. `AdjustConfig.setFbAppId`メソッドに`String`引数としてアプリIDをパスします。

<Tabs>
<Tab title="Kotlin">
<CodeBlock highlight="{range: 2}">

```kotlin
val config = AdjustConfig(this, appToken, environment)
config.setFbAppId(<fb_app_id_string>)
Adjust.initSdk(config)
```

</CodeBlock>
</Tab>
<Tab title="Java">
<CodeBlock highlight="{range: 2}">

```java
AdjustConfig config = new AdjustConfig(this, appToken, environment);
config.setFbAppId(<fb_app_id_string>);
Adjust.initSdk(config);
```

</CodeBlock>
</Tab>
</Tabs>
